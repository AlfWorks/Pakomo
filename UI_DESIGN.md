# Pakomo UI 设计说明（程序实现基线）

> 设计源在连接的 Penpot 文件 `Page 1`。Penpot 仅覆盖 **Home (v2) / Apps (v2) / Rules (v2)** 三个关键流程，并不是完整页面清单。程序还必须包含规则编辑、诊断、设置、安全与隐私等页面。
> 上排早期探索（Material3 亮色 / Soft-Tech 吉祥物）已废弃，仅留作对比，可删除。
> 视觉方向：**近乎单色 + 单一强调色 + 浅色**（Clash Meta for Android 取向）——Material 3、功能向、灰阶为主、单一蓝色强调、卡片列表、底部标签栏。无吉祥物。
> 本文件是设计 → Compose 主题的桥接，色值可直接填入 `lightColorScheme(...)`。

## 1. 设计语言

- **近乎单色**：界面以中性灰阶为主（底/卡/文字/图标/徽章/次要曲线/分隔线全部灰阶）。
- **单一强调色**：`#3B6FE0`，**只**用于有意义处——主开关开、导航激活、主按钮/FAB、运行/无冲突状态、上行曲线、关键数值、被接管的目标应用图标、最高具体度规则（左条/徽章/条件 chip 文字）。
- **克制层次**：白卡 + 1px 灰描边 `#E9EBEF` + 极轻投影 `#101828 @0.05`，圆角 16，信息密度适中。
- **数据等宽**：速率/总量/连接数/包名/域名/优先级用 Roboto Mono。
- **无彩色语义泛滥**：不用成功绿/警告琥珀/多彩徽章；正向/激活态统一用强调蓝，其余灰阶。唯一例外：危险动作（紧急恢复）用一处克制暗红字 `#B4453E`。
- **导航 = 枢纽 + 推入（CMFA 式，无底部标签栏）**：Home 是枢纽——大启停卡 + 点击入口列表；各功能页由入口**推入**（顶部返回箭头返回）。**不使用底部 Tab**。

## 2. 颜色 Tokens（近乎单色）

| Role | Hex | 用途 |
|---|---|---|
| background | `#FAFBFC` | 页面底 |
| surface / card | `#FFFFFF` | 卡片 |
| card border | `#E9EBEF` | 卡片描边（1px） |
| **accent（唯一强调）** | `#3B6FE0` | 开关开/激活/主按钮/FAB/关键值/目标高亮 |
| accent tint | `#E8EDFB` | 导航激活药丸、选中态底 |
| onSurface | `#1B1F26` | 主文字 |
| onSurfaceVariant | `#59616E` | 次文字/区块标题 |
| muted | `#98A2B3` | 占位/单位/caption/次要曲线 `#AAB0B8` |
| neutral icon | `#8C9098` | 应用图标、非激活标记 |
| neutral chip | `#F3F4F6` | 徽章/chip 灰底 |
| switch off track | `#D7DBE0` | 关闭态开关 |
| danger（唯一例外） | `#B4453E` | 仅紧急恢复等危险动作文字 |

卡片投影：`drop-shadow 0 2 8 spread0 #101828 @0.05`；FAB：`0 6 16 accent @0.4`。

## 3. 字体
- Roboto（正文/标题）+ Roboto Mono（所有数据/包名/域名/优先级）。
- 级别：屏标题 20/700、卡标题 13–15/700、StatValue 16–18/700(mono)、Body 14、Label 11–12、Caption 10。

## 4. 布局
- 屏宽 360dp，水平内边距 16dp，内容宽 328dp。
- **收敛圆角**：卡片/按钮 8dp、chip/徽章 5–6dp、开关/FAB 保持胶囊/圆形。整体只留一点点弧度。
- **纵向铺满**：内容分块分布，填满 64→~690 的可视区，末块贴近底部标签栏（间距 ≤40dp），避免顶部堆叠、底部大留白的"网页感"。
- **主操作醒目**：Home 主控为大号启停卡（accent 填充，运行中/点此停止 + 实时速率），是首页第一视觉焦点。
- **无底部标签栏**：导航靠 Home 的点击入口 + 子页返回箭头。删除 nav 后子页内容区可延伸至 ~760dp。

## 5. 三屏说明

### Home（枢纽，CMFA 式扁平）
- 顶栏：logo + Pakomo + 「非 Root 弱网模拟」副标。
- **紧凑状态条（启停）**：accentTint 底、h70。左＝状态点 + 运行中 + 副标「↑128 ↓512 KB/s · 42 连接 · 3 规则」；右＝真 Switch。**它本质就是个开关**，故只占一条，不做大英雄卡。
- **接管范围 = 首页内联分段**：`全局 / 指定应用 / 指定地址` 三项互斥，分段控件可直接切换；全局首次切入必须二次确认。
- **plain 行（扁平，无卡片底，发丝分隔线）**：选择应用（副 1 个，仅指定模式）、弱网预设（中度弱网）、规则（无冲突·accent）、日志、设置、安全与隐私。行 = 图标 + 标题 + 右侧状态 + chevron。
- **紧急恢复**：plain 红行（电源图标 + 说明），上方分隔线。
- 无底部标签栏。**首页仅状态条 + 分段两个控件带圆角，其余全扁平**（不再滥用卡片圆角）。

> 关键修正：① 启停从大卡缩为紧凑开关条，信息密度匹配、内部不贴边；② **接管范围分段提到首页可直接切换**（改名/副标提示不够——根子是不该让用户点进退出）；③ 去卡片化，plain 行 + 分隔线。实时流量曲线 + 4 格统计移入后续「监控/日志」页。

### 接管范围（Apps，推入子页）
- 顶栏：返回箭头 + 标题「接管范围」+ 刷新应用。
- **接管范围 = 三项互斥单选**：`全局 / 指定应用 / 指定地址`。指定应用显示应用列表；指定地址显示全局域名列表；全局显示风险说明。
- 搜索 + 已接管计数 + 系统应用过滤。
- **一个应用一张独立卡片**：头部一行放图标、名称、包名、域名数与接管开关。应用图标从设备读取，字母图标仅作异常回退。
- **整张应用卡头部负责展开/收起**，不再放额外折叠箭头；行尾开关只负责是否接管，点击开关不得触发展开。
- 展开区属于同一张卡片，以浅灰底与白色头部区分，不嵌套第二张卡；域名行横向铺满，删除按钮统一右对齐；「添加域名」按钮横向展开。支持多个域名。域名为空表示该应用全部流量。
- 无底部标签栏（返回箭头返回 Home）。

> **程序模型（最新确认）**：
> 1. 接管对象只在「接管范围」中维护：`全局 XOR 指定应用 XOR 指定地址`。
> 2. 指定应用的域名属于该应用卡片；指定地址的域名属于全局地址列表。二者不在规则页重复显示。
> 3. 弱网规则只描述延迟、抖动、丢包、上/下行限速；同一时间单选一条生效。

### Rules（规则列表，推入子页）
- 顶栏：返回箭头 + 标题 + 新建按钮；无需刷新（不是订阅列表）。
- **规则卡**：参考 CMFA 的配置列表。左侧单选按钮，中间规则名称与量化参数，右侧三点菜单；卡片内部上下居中。
- 规则之间互斥，不使用每卡独立开关。规则页不显示包名或域名，也不提供这些字段的编辑入口。
- 参数必须量化展示，例如 `300ms · 抖动 100ms · 丢包 5% · 512/128 Kbps`，不能只写“轻度/中度/重度”。
- 三点菜单提供编辑、复制、删除；内置规则只读，编辑时先复制。
- **FAB**（+）新建规则。
- 无底部标签栏（返回箭头返回 Home）。
- 可视化了优先级层级：包名+域名 > 包名 > 全局。

## 6. 待办（UI 后续）
- 深色主题（`darkColorScheme`）。
- 补屏：日志（连接/事件列表 + 脱敏）、设置、弱网预设编辑、规则编辑详情页、安全与隐私。
- 状态变体：Home 停止态（灰开关/暂停）、规则冲突态（softRed 卡 + 冲突提示）、列表空态。
- 组件化：TrafficChart / StatCell / AppRow(Switch) / RuleCard / BottomNav / SoftBadge → Penpot 组件 + Compose 可复用组件。
