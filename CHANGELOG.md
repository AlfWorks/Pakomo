# Changelog

本项目所有值得记录的变更都写在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 发布约定

- 平时把变更写进 `## [Unreleased]`。
- 发版前把 `[Unreleased]` 收敛成 `## [<版本>] - <日期>`；版本号与 tag 一致但去掉前缀 `v`
  （tag `v0.1.1` → `## [0.1.1]`）。
- CI 在 prod（tag）发布时**优先**读取对应版本段，作为 GitLab / GitHub Release 说明与 Novi
  更新弹窗文案；找不到该段则自动**回落**到 git log 摘取 commit（兜底，不会漏发）。
- 测试频道不发 Release，始终使用 git log。

## [Unreleased]

<!-- 在此累积下次发布的变更；发版时由发布者收敛为 "## [<版本>] - <日期>"。 -->

## [0.1.0] - 2026-08-07

- 更新对话框显示当前服务源。
- 新增自动更新开关（默认开启）。
- 修复：内核转发在连接拆除时不再打印预期内的错误/警告日志。

## [0.0.1] - 2026-08-07

- 首个发布版本：弱网整形（延迟 / 抖动 / 丢包 / 带宽）与按应用、按域名的特殊故障注入，
  基于自研 tun2socks 与本地 SOCKS 转发。
- Novi 自更新接入：从 Maven Central 消费、打包期注入更新源、信任锚烘焙与校验详情 UI；
  支持“忽略此版本”、更新源在界面中以叹号前缀脱敏。
- About 页展示版本 / 构建详情、服务源状态、检查更新与构建短哈希。
- 调试专用的外部自动化控制面（P0–P3，仅 debug 构建）。
- 包名迁移至 com.alphynia.pakomo。
