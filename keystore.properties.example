# Copy to `keystore.properties` (git-ignored) and fill in your release keystore.
# storeFile 支持绝对路径，或相对于仓库根目录的路径。
storeFile=pakomo-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=pakomo
keyPassword=YOUR_KEY_PASSWORD
